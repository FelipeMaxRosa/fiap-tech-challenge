version: '3.3'
networks:
  fastnfoodius:


services:
  mysql:
    image: mysql:latest
    container_name: mysql
    restart: always
    environment:
      MYSQL_DATABASE: fastnfoodius
      MYSQL_USER: fastnfoodius_user
      MYSQL_PASSWORD: fastnfoodius_pass
      MYSQL_ROOT_PASSWORD: root
    networks:
      - fastnfoodius
    ports:
      - '3306:3306'
    expose:
      - '3306'
    volumes:
      # - ./docker/mysql:/var/lib/mysql
      - ./src/database/db.sql:/docker-entrypoint-initdb.d/1.sql
  # app:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   volumes:
  #     - ./:/app
  #   environment:
  #     NODE_ENV: development
  #     DB_HOST: mysql
  #     DB_USERNAME: fastnfoodius_user
  #     DB_PASSWORD: fastnfoodius_pass
  #     DB_DATABASE: fastnfoodius
  #   depends_on:
  #     - mysql
  #   command: yarn dev
  #   ports:
  #     - "3000:3000"
  #   networks:
  #     - fastnfoodius

volumes:
  mysql:
